server:
  port: 8088
  servlet:
    context-path: /indentity

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/test
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
